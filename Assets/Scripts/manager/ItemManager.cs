using UnityEngine;

public class ItemManager : MonoBehaviour
{
    public GameObject weapons;

    void Start()
    {
        // var jsonTextFile = Resources.Load<TextAsset>("weapon");
        // Debug.Log(jsonTextFile);
        // weapons = JsonUtility.FromJson<Item>(jsonTextFile);

        // Debug.Log(weapons["0"]);
    }
}
